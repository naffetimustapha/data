class Solution {
    int findSum(int arr[], int n) {
        
        HashSet<Integer>hs=new HashSet<Integer>();
        for(int i=0;i<arr.length;i++){
         hs.add(arr[i]);   
        }
        int sum=0;
        for(int k:hs){
            sum=sum+k;
        }
        return sum;
    }
}
